#ifndef SG_NETGROUP_H
#define SG_NETGROUP_H 1

#include "sgMatch.h"

struct SourceMatch *newNetgroupUserMatch(const char *netgroup);
/* struct SourceMatch * newNetgroupUserMatch(const char *netgroup); */

#endif
